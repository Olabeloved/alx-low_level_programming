#include "main.h"

/**
 * _abs - checks for absolute value
 *
 * @n: the character to check
 *
 * Return: the absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
